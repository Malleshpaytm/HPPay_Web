import { MerChangePasswordPayload } from './mer-change-password-payload';

describe('MerChangePasswordPayload', () => {
  it('should create an instance', () => {
    expect(new MerChangePasswordPayload()).toBeTruthy();
  });
});
